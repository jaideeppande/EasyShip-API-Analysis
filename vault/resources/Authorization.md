---
api: Legacy API
type: resource
---
# Authorization

Part of [[Easyship Legacy API]].

## Endpoints (9)

- `GET` [[Authorize user on Easyship]] — `/oauth2/authorize`
- `POST` [[Introspect an Access Token]] — `/oauth2/introspect`
- `POST` [[Introspect an Access Token (Enterprise)]] — `/oauth2/introspect`
- `POST` [[Revoke an Access Token]] — `/oauth2/revoke`
- `POST` [[Revoke an Access Token (Enterprise)]] — `/oauth2/revoke`
- `POST` [[Create an Access Token]] — `/oauth2/token`
- `POST` [[Retrieve an Access Token]] — `/oauth2/token`
- `GET` [[Retrieve info for the Access Token]] — `/oauth2/token/info`
- `GET` [[Retrieve info for the Access Token (Enterprise)]] — `/oauth2/token/info`

## References

- [[HS codes]]
